/ = form_for [commentable, Comment.new] do |form|
/   = form.text_area :body, placeholder: "Ваше сообщение", class: "comment-text-area"
/   = form.submit id: "comment-submit", value: "Отправить"

.m-task__comments-input
  = form_for [commentable, Comment.new] do |form|
    = form.text_area :body, placeholder: "Ваш комментарий", class: "form-control m-task__comments-textarea js-auto-size"
    .d-flex.align-items-center.justify-content-between
      = form.submit id: "comment-submit", value: "Отправить", class: 'btn btn-info'

/ = form_for [commentable, Comment.new] do |f|
/   - if @comment.errors.any?
/     #error_explanation
/       h2 = "#{pluralize(@comment.errors.count, "error")} prohibited this comment from being saved:"
/       ul
/         - @comment.errors.full_messages.each do |message|
/           li = message
/
/   .field
/     = f.label :commentable
/     = f.text_field :commentable
/   .field
/     = f.label :body
/     = f.text_field :body
/   .field
/     = f.label :published
/     = f.check_box :published
/   .actions = f.submit
